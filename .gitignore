# Build artifacts
bin/
obj/

# Local database (recreated + seeded automatically on boot)
*.db
*.db-shm
*.db-wal

# Local secrets (real API keys live here, not in the committed appsettings.json)
appsettings.Development.json

# IDE / user files
.vs/
.idea/
.vscode/
*.user
*.suo

# OS
Thumbs.db
.DS_Store
